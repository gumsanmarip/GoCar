import React from "react";
import { View, Text } from "react-native";

const AddGasScreen = () => {
  return (
    <View>
      <Text>add gallons(volume), date, miles(distance), documents</Text>
    </View>
  );
};

export default AddGasScreen;
